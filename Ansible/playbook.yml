---
- name: Execute shell commands on another EC2 instance
  hosts: your_ec2_instance
  gather_facts: no

  tasks:
    - name: Run shell commands
      become: yes
      shell:
        cmd: |
          echo "Hello from {{ ansible_host }}"